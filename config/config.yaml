app:
  name: "Custom LLM" # Name of the application
  version: "1.0.0" # Version of the application

api:
  key: "${API_KEY}" # API key for authentication

logging:
  level: "INFO"
  log_directory: "logs"
  log_file_format: "[%(asctime)s] %(name)s - %(Levelname)s - %(message)s"